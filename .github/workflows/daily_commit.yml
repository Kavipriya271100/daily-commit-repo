name: Daily Commit

on:
  schedule:
    - cron: '30 2 * * *' # Runs daily at 2:30 AM UTC.  Adjust as needed.

jobs:
  daily_update:
    runs-on: ubuntu-latest
    steps:
      - name: Your Email (kavipriya.manivannan@straive.com)
        run: |
          echo "This action runs daily."

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Automated daily commit"
          file_pattern: "" # Commit all changes.  If you have specific files, list them here.
